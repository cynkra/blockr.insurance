% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio.R
\name{run_portfolio}
\alias{run_portfolio}
\title{Run a rating engine over every policy in a portfolio}
\usage{
run_portfolio(dir, engine = engine_property, params = NULL, overwrite = FALSE)
}
\arguments{
\item{dir}{Portfolio directory.}

\item{engine}{Engine function with signature \code{engine(inputs, params)}
returning \verb{list(premium = <data.frame>)}. Defaults to
\code{\link[=engine_property]{engine_property()}}.}

\item{params}{Optional named list of parameter tables shared across all
policies. If \code{NULL}, the engine's own defaults are used.}

\item{overwrite}{If \code{FALSE} (default), policies whose \verb{outputs/} folder
already contains CSVs are skipped (their cached output is still picked
up for the portfolio aggregate).}
}
\value{
Invisibly, a character vector of policy ids that were (re)run.
}
\description{
For each policy folder, reads the \verb{inputs/} CSVs, calls \code{engine(inputs, params)}, and writes the resulting \code{premium} table to
\verb{<policy>/outputs/premium.csv}. Once every policy is run, also writes a
portfolio-root \code{premium.csv} that row-binds every per-policy result with
an added \code{policy_id} column.
}
