% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio_inputs_block.R
\name{new_portfolio_inputs_block}
\alias{new_portfolio_inputs_block}
\title{Portfolio inputs block}
\usage{
new_portfolio_inputs_block(dir = default_portfolio_dir(), ...)
}
\arguments{
\item{dir}{Portfolio directory. Defaults to the bundled
\verb{inst/extdata/portfolio-property/} 5-policy fixture.}

\item{...}{Forwarded to \code{\link[blockr.core:new_data_block]{blockr.core::new_data_block()}}.}
}
\value{
A blockr data block.
}
\description{
Data block that reads every policy's \verb{inputs/} CSVs (locations + claims)
and exposes them as a single \code{dm} with a \code{policy_id} column on each
table. Use this with \code{\link[=new_price_block]{new_price_block()}} to drive a SAA-style workbench
where the engine partitions by \code{policy_id} and runs once per policy.
}
\details{
Pair with the bundled fixtures via \code{\link[=default_portfolio_dir]{default_portfolio_dir()}} (5 policies,
69 locations) — or point at any directory with the same shape.
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.dm)
  library(blockr.insurance)
  serve(
    new_board(
      blocks = list(
        inputs  = new_portfolio_inputs_block(),
        pricing = new_price_block()
      ),
      links = c(
        new_link("inputs", "pricing", "inputs")
      )
    )
  )
}

}
