% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_property_v2.R
\name{engine_property_v2}
\alias{engine_property_v2}
\title{Property pricing engine — v2 with CAT loading}
\usage{
engine_property_v2(inputs, params = NULL)
}
\arguments{
\item{inputs}{Named list (or \code{dm}) with two tables:
\itemize{
\item \code{locations}: one row per insured location.
Required columns: \code{location_id}, \code{country}, \code{tiv}, \code{sra_adj},
\code{deductible}, \code{limit}.
\item \code{claims}: one row per claim.
Required columns: \code{claim_id}, \code{date_of_loss}, \code{country}, \code{paid},
\code{outstanding}.
}}

\item{params}{Named list (or \code{dm}) of parameter tables. If \code{NULL}, the
package defaults from \link{property_params} are used. Expected tables:
\itemize{
\item \code{country_factor}: \code{country}, \code{country_factor}.
\item \code{base_rate}: \code{country}, \code{base_rate}.
\item \code{expenses}: a single-row table with \code{expenses_factor}.
}}
}
\value{
A named list with one element, \code{premium} — same schema as
\code{\link[=engine_property]{engine_property()}}. v2's \code{risk_premium} and \code{model_price} are
loaded per-location: \code{column_v2 = column_v1 * cat_factor}. The
pre-loading columns (\code{base_premium}, \code{layer_share}, \code{exposure_premium},
\code{experience_premium}) are identical to v1. The waterfall step from
\code{risk_premium} to \code{model_price} therefore shows the CAT effect on top
of the standard \code{expenses_factor} markup.
}
\description{
Sibling of \code{\link[=engine_property]{engine_property()}} (not a successor). Identical signature and
output schema; the only difference is a per-country natural-catastrophe
loading applied to \code{exposure_premium} before experience aggregation.
}
\details{
Use the two engines side-by-side via \code{\link[=new_price_block]{new_price_block()}} (gear popover) or
through \code{compare(v1, v2)} workflows. Demo target: SAA 2026 property
workbench, where the CAT loading is the lever the walkthrough toggles to
show how a new engine version reprices a book.

The CAT loading reads \code{params$cat_factor} (a \code{country} x \code{cat_factor}
table). If absent or the country isn't in the table, the loading defaults
to 1 — i.e. v2 collapses to v1 numerically. This makes the engine safe to
run against the older \link{property_params_comparison} fixture (which doesn't
include \code{cat_factor}).

Implementation note: CAT is applied \strong{after} the v1 allocation step,
not as a multiplier on \code{exposure_premium}. The proposal's calculation
flow (Fig 2.1) places \code{mod_cat} between layering and experience, but
applying it there would interfere with v1's exposure-weighted allocation
of policy-grain aggregates back to locations (a high-CAT country could
end up with a \emph{smaller} per-location share if its loading is below the
portfolio-weighted-average loading). Applying CAT post-allocation gives
the cleaner per-location story the demo needs: Italy locations on v2
are exactly \code{cat_factor[Italy]} times their v1 model_price.
}
\examples{
inp <- list(
  locations = blockr.insurance::property_locations,
  claims    = blockr.insurance::property_claims
)
out_v1 <- engine_property(inp)
out_v2 <- engine_property_v2(inp)
# Italy locations get a 1.30 cat_factor; UK 1.15; etc.
merge(
  out_v1$premium[, c("location_id", "country", "exposure_premium")],
  out_v2$premium[, c("location_id", "exposure_premium")],
  by = "location_id", suffixes = c("_v1", "_v2")
) |> head()

}
\seealso{
\code{\link[=engine_property]{engine_property()}} (v1), \code{\link[=new_price_block]{new_price_block()}}, \link{property_params}.
}
