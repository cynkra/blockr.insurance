% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio.R
\name{read_portfolio_inputs}
\alias{read_portfolio_inputs}
\title{Read all policies' inputs as a long-format named list}
\usage{
read_portfolio_inputs(dir = default_portfolio_dir())
}
\arguments{
\item{dir}{Portfolio directory. Defaults to the bundled
\verb{inst/extdata/portfolio-property/} 5-policy fixture.}
}
\value{
Named list with two data frames, \code{locations} and \code{claims},
each carrying a \code{policy_id} column.
}
\description{
Reads every policy's \verb{inputs/} CSVs (locations + claims) and row-binds
them across policies, adding a \code{policy_id} column to each table. The
result feeds \code{\link[=engine_property]{engine_property()}} / \code{\link[=engine_property_v2]{engine_property_v2()}} in their
multi-policy mode (the engine partitions by \code{policy_id} and runs once
per policy). Use this with \code{\link[=new_price_block]{new_price_block()}} to drive the SAA
workbench from the bundled portfolio fixtures.
}
