% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio.R
\name{portfolio}
\alias{portfolio}
\title{Portfolio runs over many policies}
\description{
A \emph{portfolio} is a directory of policies, each in its own subfolder. Every
policy folder has an \verb{inputs/} subdirectory with one CSV per input table
(e.g. \code{locations.csv}, \code{claims.csv}) and an \verb{outputs/} subdirectory which
\code{\link[=run_portfolio]{run_portfolio()}} populates with one CSV per result table (currently just
\code{premium.csv}). Once every policy is run, \code{\link[=run_portfolio]{run_portfolio()}} also writes a
single portfolio-root \code{premium.csv} that binds every per-policy result
with an added \code{policy_id} column.
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{my-portfolio/
  premium.csv                       # portfolio aggregate (added by run_portfolio)
  policy-001/
    inputs/locations.csv
    inputs/claims.csv
    outputs/premium.csv
  policy-002/
    ...
}\if{html}{\out{</div>}}

The functions in this file are pure R (no Shiny). The blockr wrappers that
expose them in a dashboard live in \code{R/portfolio_blocks.R}.
}
