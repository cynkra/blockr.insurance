% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/price_block.R
\name{new_price_block}
\alias{new_price_block}
\title{Price block — pick an engine, run on inputs (and optional parameters)}
\usage{
new_price_block(
  engine = available_engines()[[1L]],
  package = "blockr.insurance",
  ...
)
}
\arguments{
\item{engine}{Character; default engine function name. Must be one of
\code{\link[=available_engines]{available_engines()}}. Defaults to the first available
(\code{"engine_property"}).}

\item{package}{Character; package the engine lives in. Defaults to
\code{"blockr.insurance"}. May be \code{""} for a function in the global
environment.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A blockr transform block.
}
\description{
Generic blockr wrapper around a \emph{rating engine} — any function with the
signature \code{engine(inputs, params) -> outputs}, where \code{inputs} and
\code{outputs} are named lists of data frames (or \code{dm} objects) and \code{params} is
an optional named list of parameter tables.
}
\details{
The engine is \strong{selectable at runtime} via a dropdown in the block UI.
Toggling the engine re-evaluates the block — same inputs, different
calculation. This is the lever the SAA workbench demo points at when
walking through "version 1 vs version 2" of a pricing engine.

The block is variadic and accepts up to two upstream inputs:
\itemize{
\item \code{inputs} (required): a \code{dm} or named list with the engine's input
tables. Wire it with \code{new_link(..., input = "inputs")}.
\item \code{params} (optional): a \code{dm} or named list with the engine's parameter
tables. Wire it with \code{new_link(..., input = "params")}. If absent,
the engine's own default parameters are used.
}

The engine's output (a named list of tables) is wrapped in a \code{dm} so the
block can be chained with \code{new_dm_pull_block()} to extract individual
result tables for inspection or downstream visualisation.
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.dm)
  library(blockr.insurance)
  serve(
    new_board(
      blocks = list(
        locations = new_dataset_block("property_locations",
                                      "blockr.insurance"),
        claims    = new_dataset_block("property_claims",
                                      "blockr.insurance"),
        inputs    = new_dm_block(infer_keys = FALSE),
        pricing   = new_price_block()
      ),
      links = c(
        new_link("locations", "inputs",  "locations"),
        new_link("claims",    "inputs",  "claims"),
        new_link("inputs",    "pricing", "inputs")
      )
    )
  )
}

}
