% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_property.R
\name{engine_property}
\alias{engine_property}
\title{Property pricing engine}
\usage{
engine_property(inputs, params = NULL)
}
\arguments{
\item{inputs}{Named list (or \code{dm}) with two tables:
\itemize{
\item \code{locations}: one row per insured location.
Required columns: \code{location_id}, \code{country}, \code{tiv}, \code{sra_adj},
\code{deductible}, \code{limit}.
\item \code{claims}: one row per claim.
Required columns: \code{claim_id}, \code{date_of_loss}, \code{country}, \code{paid},
\code{outstanding}.
}}

\item{params}{Named list (or \code{dm}) of parameter tables. If \code{NULL}, the
package defaults from \link{property_params} are used. Expected tables:
\itemize{
\item \code{country_factor}: \code{country}, \code{country_factor}.
\item \code{base_rate}: \code{country}, \code{base_rate}.
\item \code{expenses}: a single-row table with \code{expenses_factor}.
}}
}
\value{
A named list with one element, \code{premium}: a data frame with one
row per insured location and the following columns:
\itemize{
\item Location attributes carried through: \code{location_id}, \code{country},
\code{postcode}, \code{tiv}, \code{sra_adj}, \code{deductible}, \code{limit}.
\item Joined parameters: \code{country_factor}, \code{base_rate}.
\item Per-location: \code{base_premium}, \code{layer_share}, \code{exposure_premium}.
\item Broadcast aggregates: \code{total_incurred}, \code{n_claim_years},
\code{experience_premium}, \code{exposure_premium_total}, \code{credibility},
\code{risk_premium}, \code{expenses_factor}, \code{model_price}.
}
}
\description{
A minimal property-insurance rating engine. Pure R, no Shiny: takes a list
of input tables (locations and claims) plus an optional list of parameter
tables, and returns a single wide table at the location grain. Per-location
columns (e.g. \code{base_premium}, \code{exposure_premium}) carry their location
value; portfolio-aggregate columns (e.g. \code{risk_premium}, \code{model_price}) are
broadcast onto every row so a single crossfilter slice keeps the full
comparison schema available downstream.
}
\details{
This is the standard rating-engine signature used in this package: a pure
function \code{engine(inputs, params) -> outputs} where \code{outputs} is a named
list of data frames. \code{\link[=new_rating_engine_block]{new_rating_engine_block()}} turns any such function
into a blockr block.
}
\examples{
inp <- list(
  locations = blockr.insurance::property_locations,
  claims    = blockr.insurance::property_claims
)
out <- engine_property(inp)
head(out$premium)

}
