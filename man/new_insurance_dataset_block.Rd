% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insurance_dataset_block.R
\name{new_insurance_dataset_block}
\alias{new_insurance_dataset_block}
\title{Insurance dataset block}
\usage{
new_insurance_dataset_block(dataset = "motor_portfolio", ...)
}
\arguments{
\item{dataset}{Character; default dataset. Must be one of the
\code{data.frame}-eligible datasets shipped by \code{blockr.insurance}.}

\item{...}{Forwarded to \code{\link[blockr.core:new_data_block]{blockr.core::new_dataset_block()}}.}
}
\value{
A blockr data block.
}
\description{
Thin wrapper around \code{\link[blockr.core:new_data_block]{blockr.core::new_dataset_block()}} with the dataset
package locked to \code{blockr.insurance}. The base \code{new_dataset_block()}
exposes only the dataset dropdown — \code{package} is a constructor argument
with no UI counterpart, so an end user adding the stock block in a
running app cannot switch away from the \code{datasets} default. This wrapper
fixes \code{package = "blockr.insurance"} and defaults \code{dataset} to
\code{motor_portfolio} so the block can be added interactively and immediately
yields a usable insurance dataset.
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.insurance)
  serve(
    new_board(
      blocks = list(motor = new_insurance_dataset_block())
    )
  )
}

}
